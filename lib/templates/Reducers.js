import {
    // @rgen-import    
    #####_INIT
} from '../Actions/Action#####'

export const Reducer##### = (state = {
    
}, action) => {
    switch (action.type) {
        // @rgen-case        
        case #####_INIT:
            return {
                ...state
            }
        default:
            return state
    }
}
