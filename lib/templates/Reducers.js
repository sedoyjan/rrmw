import {
    #####_INIT
} from '../Actions/Actions#####'

export const Reducer##### = (state = {
    
}, action) => {
    switch (action.type) {
        case #####_INIT:
            return {
                ...state
            }
        default:
            return state
    }
}
