// @flow

import {
    // @rgen-import    
} from '../Actions/Action#####'

export type IReducer#####_State = {

}

let initialState: IReducer#####_State = {
    
}

export const Reducer##### = (state = initialState, action) => {
    switch (action.type) {
        // @rgen-case
        default:
            return state
    }
}
