// @flow

import {
    // @rgen-import    
} from '../Actions/ActionReport'

export type IReducerReport_State = {

}

let initialState: IReducerReport_State = {
    
}

export const ReducerReport = (state = initialState, action) => {
    switch (action.type) {
        // @rgen-case
        default:
            return state
    }
}
